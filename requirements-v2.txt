# Optional V2 retrieval dependencies. Install only when running V2 Unstructured/Cohere experiments.
cohere>=5.0.0
python-dotenv>=1.0.0
unstructured[pdf]>=0.16.0
# Required on Windows Python 3.13 because the unstructured[pdf] marker does
# not pull it automatically, but the PDF partitioner imports it.
unstructured-inference>=1.6.12,<2.0.0
# Keep optional PDF/OCR dependencies compatible with existing Streamlit/OR-Tools.
protobuf>=6.33.1,<6.34
grpcio-status<1.82

# Optional local demo UI.
streamlit>=1.36.0
