# Archived failed Phase 7 held-out retrieval attempt

Reason: technical schema/write failure in the Phase 7 final-evaluation artifact layer.

The retrieval run completed, but integrity validation failed because the artifact canonicalizer did not emit explicit empty ranking trace fields for an unsupported-period case with a required report ID. No retrieval configuration was changed and no held-out failures were inspected for optimisation.

